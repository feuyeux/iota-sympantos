# docs/debugging.md poster prompt

Use `gpt-image-2-style-library`: `pen-and-ink technical story poster`, `debugging workshop`, `hand-drawn developer desk`, `fine cross-hatching`, `warm paper texture`.

Create a vertical poster for the document `iota-sympantos debugging guide`.

Scene: an engineer at a workbench with CodeLLDB tools. A Rust CLI machine is open for inspection with six labeled access panels: `cli/mod.rs`, `engine/mod.rs`, `acp/client.rs`, `tui/loop.rs`, `tui/render.rs`, `runtime_event/mod.rs`. Breakpoints glow as small red dots. A side panel shows seven debug configurations as brass tags: Debug TUI, Debug Run, Debug Run with Daemon, Debug Check, Debug MCP Context, Debug MCP Fun, Debug Bench. At the bottom, a recovery station shows `RUST_LOG=debug`, `RUST_BACKTRACE=1`, log files at `~/.i6/logs/`, and a terminal-reset lever.

Composition: portrait poster, 2:3 aspect ratio. Top to bottom: prerequisites → debug configs → breakpoints → variable inspection → TUI debugging → ACP subprocess boundary. Small keyboard strip with `F5`, `F10`, `F11`, `Shift+F5`. Title `Debugging iota-sympantos` at top.

Style: black ink pen illustration, crisp linework, cross-hatched shadows, readable labels, magenta accent on breakpoint dots and the active debug path. Practical and clean, not cartoonish.

Mood: focused repair manual — complex but understandable.

Negative prompt: chaotic code wall, unreadable text, photorealistic office, neon cyberpunk, distorted keyboards, fake stack traces, old debug names (Debug Bench Cold, Debug Context MCP Sidecar, Debug Fun MCP Server), obsolete modules (tui.rs single-file, telemetry/console.rs).